name: GameLand image lint & build & push on main branch

on:
  pull_request:
    branches:
      - main

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    environment: Production

    env:
      IMAGE_NAME: gameland
      HUB_IMAGE_NAME: ${{ secrets.DOCKERHUB_USERNAME }}/$IMAGE_NAME

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install dependencies
        run: npm ci
        working-directory: gameland


      - name: Lint
        run: npm run lint
        working-directory: gameland


      - name: Build
        run: npm run build
        working-directory: gameland


      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Extract version from PR title
        id: version
        run: |
          TITLE="${{ github.event.pull_request.title }}"
          if [[ "$TITLE" == *"[patch]"* ]]; then
            echo "type=patch" >> $GITHUB_OUTPUT
          elif [[ "$TITLE" == *"[minor]"* ]]; then
            echo "type=minor" >> $GITHUB_OUTPUT
          elif [[ "$TITLE" == *"[major]"* ]]; then
            echo "type=major" >> $GITHUB_OUTPUT
          else
            echo "type=patch" >> $GITHUB_OUTPUT  # default
          fi

      - name: Generate Docker image tag
        id: tag
        run: |
          VERSION_FILE=version.txt
          [ ! -f $VERSION_FILE ] && echo "1.0.0" > $VERSION_FILE
          CURRENT_VERSION=$(cat $VERSION_FILE)
          IFS='.' read -r MAJOR MINOR PATCH <<< "$CURRENT_VERSION"

          case "${{ steps.version.outputs.type }}" in
            major) MAJOR=$((MAJOR+1)); MINOR=0; PATCH=0 ;;
            minor) MINOR=$((MINOR+1)); PATCH=0 ;;
            patch) PATCH=$((PATCH+1)) ;;
          esac

          NEW_VERSION="$MAJOR.$MINOR.$PATCH"
          echo "$NEW_VERSION" > $VERSION_FILE
          echo "tag=$NEW_VERSION" >> $GITHUB_OUTPUT

      - name: Build Docker image
        uses: docker/build-push-action@v4
        with:
          context: gameland
          push: false
          tags: ${{ env.IMAGE_NAME }}:${{ steps.tag.outputs.tag }}

      - name: Minify Docker image with docker-slim
        uses: kitabisa/docker-slim-action@v1
        with:
          target: ${{ env.IMAGE_NAME }}:${{ steps.tag.outputs.tag }}
          tag: ${{ steps.tag.outputs.tag }}-slim

      - name: Tag slim image as version and latest
        run: |
          docker images
          docker tag $IMAGE_NAME:${{ steps.tag.outputs.tag }}-slim $HUB_IMAGE_NAME:${{ steps.tag.outputs.tag }}
          docker tag $IMAGE_NAME:${{ steps.tag.outputs.tag }}-slim $HUB_IMAGE_NAME:latest

      - name: Push Docker images
        run: |
          docker push $HUB_IMAGE_NAME:${{ steps.tag.outputs.tag }}
          docker push $HUB_IMAGE_NAME:latest
